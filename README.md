# Image to FITS directory converter

Quick script put together for converting images to FITS maps of R, G, B + Grayscale

## Installing

```
# Clone...
cd img2fits
pip install -r requirements.txt
```
